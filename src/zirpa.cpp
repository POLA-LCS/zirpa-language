#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

string zirpify(string text){
    
}

int main(){
    
    fstream file;
    file.open("input.txt", ios::in);
    if(!file.is_open()){
        cerr << "Can't open the file \"input.txt\"";
    }


    
    return 0;
}